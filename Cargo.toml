[package]
name = "discord-bot"
version = "0.1.0"
edition = "2021"

[dependencies]
llm = { version = "1.0.7", features = ["ollama"] }
serenity = { version = "0.12", features = ["temp_cache"] }
tokio = { version = "1.21.2", features = ["full", "macros", "rt-multi-thread"] }

